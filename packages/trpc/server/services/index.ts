/**
 * Service singletons live here in the starter shape. ChaiForm's routers query
 * `db` directly (no class-based service layer), so this module is intentionally
 * empty. Kept to match the @repo/trpc package layout.
 */
export {};
